{
  "name": "MerchTrack Dev Container",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "nvm install 22.12 && nvm use 22.12 && npm i -g pnpm && pnpm install && echo 'Please create an .env file with the necessary environment variables.'",
  "extensions": [
    "dbaeumer.vscode-eslint",
    "github.copilot"
  ],
  "settings": {
    "remote.containers.defaultExtensions": [
      "dbaeumer.vscode-eslint",
      "github.copilot"
    ],
    "remote.containers.machineType": "4-core"
  }
}
